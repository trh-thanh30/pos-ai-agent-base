export interface Category {
  id: string;
  store_id: string;
  name: string;
  description?: string;
  createdAt: string;
  updatedAt: string;
}
export interface Tag {
  id: string;
  store_id: string;
  name: string;
  description?: string;
  createdAt: string;
  updatedAt: string;
}
