export interface RewardPointInfo {
  id: string;
  store_id: string;
  convert_to: string;
  convert_rate: number;
  point_value: number;
  is_apply: boolean;
  description: string;
  createdAt: string;
  updatedAt: string;
}
