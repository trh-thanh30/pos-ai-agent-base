/**
 * Server Error Classes (5xx)
 * Exports all server error classes that extend BaseServerError
 */

// Base class
export { BaseServerError } from './base';

// Server error classes
export { InternalServerError } from './internal-server-error';
